---
alwaysApply: true
description: core 包源码架构、模块拆分与编码约束
---

# Core 包架构

源码位于 [packages/core/src/](mdc:packages/core/src/index.ts)，按职责拆分为 6 个模块，全部为具名导出以便 tree-shaking。

## 模块职责

| 文件 | 职责 |
| --- | --- |
| [config.ts](mdc:packages/core/src/config.ts) | `defaultOptions`（最佳实践默认值）+ `WatermarkOptions` / `ContainerInput` 类型 |
| [types.ts](mdc:packages/core/src/types.ts) | `ResolvedOptions`（运行时已解析）+ `WatermarkInstance` 公共接口 |
| [utils.ts](mdc:packages/core/src/utils.ts) | 零依赖工具函数：`uid` / `isFunction` / `resolve` / `assign` / `deg2rad` / `supportsMutationObserver` / `supportsResizeObserver` / `cssToPx` / `toElement` |
| [canvas.ts](mdc:packages/core/src/canvas.ts) | `drawTile`：Canvas 2D 绘制单个水印瓦片 → PNG data URL |
| [watermark.ts](mdc:packages/core/src/watermark.ts) | `Watermark` 主类（公开）+ `WatermarkImpl`（内部实现） |
| [index.ts](mdc:packages/core/src/index.ts) | 统一出口，导出顺序按字母序（受 eslint `perfectionist/sort-imports` 约束） |

## 关键设计原则

1. **零运行时依赖** — core 的 `package.json` 不允许出现 `dependencies`，只允许 `devDependencies`
2. **SSR 安全** — 所有 DOM 访问必须用 `typeof document !== 'undefined'` / `typeof window !== 'undefined'` 守卫（见 [canvas.ts](mdc:packages/core/src/canvas.ts) 与 [watermark.ts](mdc:packages/core/src/watermark.ts)）
3. **Android 6 兼容** — 禁用 `OffscreenCanvas` / `Proxy` / `Reflect`；`MutationObserver`（Chrome 18+）可用，`ResizeObserver` 缺失时回退到 `window.resize`
4. **单次绘制** — Canvas 仅在 `create` / `update` 时绘制一次，resize 期间靠 CSS `background-repeat` 平铺，**不要**在 resize 回调里重绘 canvas
5. **防篡改异步化** — 恢复动作通过 `setTimeout(0)` + `reentrancyGuard` 防止与用户连续操作死锁。所有内部修改 style/childList 的方法（`paint`、`update`、`show`、`hide`）都必须通过 `withGuard()` 包裹，保证自身触发的 MutationObserver 回调被忽略
6. **多实例隔离** — 每个实例独立 `uid`、独立 DOM 节点（`data-watermark` 属性）、独立 observer

## 实例 API 契约

`WatermarkInstance`（见 [types.ts](mdc:packages/core/src/types.ts)）暴露：
- 属性：`id` / `options` / `isMounted`（均 `readonly`）
- 方法：`render()` / `update(next?)` / `show()` / `hide()` / `destroy()`
- `hide()` 保留 observer（防篡改仍生效），`destroy()` 才彻底释放
- `update()` 无参数为 no-op

## 修改约束

- 新增选项时必须同步更新 [config.ts](mdc:packages/core/src/config.ts) 的 `defaultOptions`、[types.ts](mdc:packages/core/src/types.ts) 的 `ResolvedOptions`、[playground/guide/options.md](mdc:playground/guide/options.md) 文档、以及 [tests/src/index.test.ts](mdc:tests/src/index.test.ts)
- `ResolvedOptions` 中 `container` 必须是 `HTMLElement | null`（已解析），不要把函数/选择器留在运行时选项里
- `WatermarkImpl` 是内部类不导出，外部只能通过 `Watermark.create()` 获取实例
