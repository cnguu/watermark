---
alwaysApply: true
description: 开发工作流、常用命令与提交前检查清单
---

# 开发工作流

## 常用命令

```bash
# 安装
pnpm install

# 开发
pnpm dev            # 监听构建 core（tsdown --watch）
pnpm play           # 启动 VitePress 文档（含热更新）

# 质量检查
pnpm typecheck      # tsc --noEmit
pnpm lint           # eslint .
pnpm lint:fix       # eslint . --fix
pnpm test           # vitest run

# 构建
pnpm build          # 构建 core → packages/core/dist
pnpm play:build     # 构建文档 → playground/.vitepress/dist
pnpm play:preview   # 预览文档构建产物

# 发布
pnpm release        # bumpp 升级 core 版本号 + tag
pnpm publish:core   # 构建 + 发布到 npm
```

## 开发循环

1. 修改 core 源码 → `pnpm dev` 自动重建
2. 修改文档/演示 → `pnpm play` 热更新
3. 写测试 → `pnpm test:watch`
4. 提交前 lefthook 自动跑 lint + typecheck
5. push 前自动跑 test

## 提交前检查清单

提交前确保以下全部通过：

```bash
pnpm typecheck && pnpm lint && pnpm test && pnpm build
```

如果是文档变更，额外跑：

```bash
pnpm play:build
```

## 修改时的同步要求

| 改动 | 需同步更新 |
| --- | --- |
| 新增/修改 core 选项 | [config.ts](mdc:packages/core/src/config.ts) + [types.ts](mdc:packages/core/src/types.ts) + [playground/guide/options.md](mdc:playground/guide/options.md) + [tests](mdc:tests/src/index.test.ts) + [playground 演示](mdc:playground/playground/index.md) |
| 新增/修改实例方法 | [types.ts](mdc:packages/core/src/types.ts) + [watermark.ts](mdc:packages/core/src/watermark.ts) + [playground/api/watermark.md](mdc:playground/api/watermark.md) + 测试 |
| 新增工具函数 | [utils.ts](mdc:packages/core/src/utils.ts) + [index.ts](mdc:packages/core/src/index.ts) 导出 + [playground/api/utils.md](mdc:playground/api/utils.md) |
| 升级依赖 | [pnpm-workspace.yaml](mdc:pnpm-workspace.yaml) catalog + `pnpm-lock.yaml` |
| 新增文档页 | [playground/.vitepress/config.ts](mdc:playground/.vitepress/config.ts) 侧边栏注册 |

## 约束

- **不要**直接 commit 到 main（除非是小修复）
- **不要**跳过 git hooks（`--no-verify`），除非明确必要并说明原因
- **不要**手动改 `pnpm-lock.yaml`，用 `pnpm install` 生成
- **不要**提交 `dist/`（已在 .gitignore）—— `pnpm publish:core` 会先构建
- **不要**提交 `*.tsbuildinfo` / `.eslintcache`（已 gitignore）
