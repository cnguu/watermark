---
alwaysApply: true
description: 项目整体结构、pnpm monorepo 工作区布局与各包职责
---

# 项目结构

`@cnguu/watermark` 是一个纯 TypeScript、零运行时依赖的 DOM 水印库，使用 pnpm monorepo 组织。

## 工作区布局

```
watermark/
├─ packages/core/       # 核心库 @cnguu/watermark（发布到 npm）
├─ playground/          # VitePress 文档站点 + 在线演示（私有）
├─ tests/               # Vitest 测试包（私有，jsdom 环境）
├─ pnpm-workspace.yaml  # 工作区与 catalog 版本锁定
├─ tsconfig.json        # 根 TypeScript 配置（含 DOM lib）
├─ eslint.config.js     # @antfu/eslint-config + package-json 插件
├─ lefthook.yml         # pre-commit lint+typecheck / pre-push test
└─ bump.config.ts       # bumpp 版本发布配置（仅 bump packages/core/package.json）
```

工作区定义见 [pnpm-workspace.yaml](mdc:pnpm-workspace.yaml)，包含三个包：
- `packages/*` → `@cnguu/watermark`
- `playground` → `@cnguu/watermark-playground`
- `tests` → `@cnguu/watermark-tests`

## 包间依赖

- `playground` 与 `tests` 都通过 `workspace:*` 依赖 `@cnguu/watermark`
- `tests/tsconfig.json` 使用 `customConditions: ["source"]` 直接引用 core 的源码而非 dist，便于调试

## 核心约束

- **不要**在 `packages/core` 引入任何运行时第三方依赖（保持零依赖）
- **不要**修改 `pnpm-workspace.yaml` 的 `catalog:` 版本号除非明确升级
- `bump.config.ts` 只 bump `packages/core/package.json`，monorepo 根 package.json 保持 `0.0.0`
